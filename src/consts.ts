/** Primary accent colour used for active/selected UI controls. */
export const BLUE = "#007bff";

/** Secondary accent colour used for inverse-mode indicator. */
export const ORANGE = "#ffa500";

/** Background tint applied to a selected EO row. */
export const EO_SELECTED_BG = "#e0e0e0";

/** Milliseconds before a single-click timeout is treated as a single click
 *  rather than the start of a double-click. */
export const DOUBLE_CLICK_DELAY_MS = 200;

export const Spacing = {
    XS: "6px",
    SM: "8px",
    MD: "10px",
} as const;
